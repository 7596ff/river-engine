---
phase: 03-sync-protocol-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - workspace/README.md
autonomous: true
requirements: [INST-01, INST-02]

must_haves:
  truths:
    - "Workspace README mentions sync protocol"
    - "README links to sync.md for detailed sync instructions"
    - "Introduction explains PR-style flow conceptually"
  artifacts:
    - path: "workspace/README.md"
      provides: "Brief sync protocol introduction with link to sync.md"
      contains: "sync"
  key_links:
    - from: "workspace/README.md"
      to: "workspace/shared/sync.md"
      via: "documentation reference"
      pattern: "sync\\.md"
---

<objective>
Update workspace README.md to mention sync protocol and link to detailed sync.md documentation, maintaining the README's philosophical tone while adding practical sync guidance.

Purpose: Integrate sync documentation into workspace orientation. Agents reading README learn that sync exists and where to find instructions, without cluttering README with technical details.

Output: Updated workspace/README.md with sync protocol mention and link to workspace/shared/sync.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sync-protocol-documentation/03-CONTEXT.md
@workspace/README.md
</context>

<interfaces>
<!-- Existing README.md structure and tone -->

From workspace/README.md:
- Philosophical tone: "You are one half of a dyad", "The gap between perspectives is the point"
- Explains workspace structure conceptually before diving into directories
- Links to other workspace files for details (e.g., "The zettelkasten.md file describes the method")
- Sections: Where You Are, The Workspace, Why Two, Memory, Ground
- Does not include step-by-step instructions (delegates to reference.md, role files)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add sync protocol mention to workspace/README.md</name>
  <files>workspace/README.md</files>

  <read_first>
    - workspace/README.md (current structure, tone, and content)
    - .planning/phases/03-sync-protocol-documentation/03-CONTEXT.md (D-03: README update guidance)
  </read_first>

  <action>
Update `workspace/README.md` to mention the sync protocol, maintaining the file's philosophical tone and structure. The addition should fit naturally into the existing narrative without disrupting flow.

**Integration point (per D-03):**
Add sync mention in the `## The Workspace` section, after the description of `shared/reference.md` and before moving to other directories. The addition should:
1. Briefly explain that agents sync using git
2. Mention the PR-style flow conceptually (not with git commands)
3. Link to `shared/sync.md` for detailed instructions
4. Maintain README's philosophical voice (not a technical manual)

**Specific text to add:**

After the paragraph about `shared/reference.md` (currently: "The `shared/` directory holds reference material that both workers use..."), insert:

```markdown
The `shared/sync.md` file documents how you and your partner synchronize changes. You work on separate branches in isolated worktrees. When you're ready to share your work, you merge to main. Your partner pulls to see what you've done. This is a "pull request" style workflow, but purely local — git commands, no GitHub. The file describes when to commit, when to sync, and how to resolve conflicts when both of you modify the same file. The mechanics are there. The protocol is deliberate.
```

**Tone guidance:**
- Match README's existing voice: direct address ("you", "your partner"), conceptual explanations, acknowledgment of coordination challenges
- Avoid technical jargon in README (save git flags and commands for sync.md)
- Frame sync as collaboration, not just version control ("how you and your partner synchronize")
- Acknowledge conflict possibility without dwelling on it ("when both of you modify the same file")

**Location in file:**
- Section: `## The Workspace`
- After: Paragraph about `shared/reference.md` (line ~21-22 currently)
- Before: Paragraph about `conversations/` directory (line ~23 currently)

Do not modify any other sections of README.md. The additions about sync are confined to the Workspace section.
  </action>

  <verify>
    <automated>
# Verify sync.md is mentioned in README
grep -q "sync.md" workspace/README.md && echo "✓ sync.md referenced" || echo "✗ sync.md not mentioned"

# Verify sync concept is introduced
grep -q "synchronize" workspace/README.md && echo "✓ Synchronization concept present" || echo "✗ Synchronization not mentioned"

# Verify PR-style flow mentioned
grep -q "pull request\|merge to main" workspace/README.md && echo "✓ PR-style flow described" || echo "✗ PR-style flow missing"

# Verify philosophical tone maintained (spot check for agent-oriented language)
grep -c "you\|your partner" workspace/README.md | awk '{if($1>=10) print "✓ Agent-oriented tone maintained"; else print "✗ Tone may have shifted"}'

# Verify no git commands leaked into README
if grep -q "git commit\|git pull\|git merge" workspace/README.md; then
  echo "✗ Technical commands in README (should be in sync.md only)"
else
  echo "✓ No technical commands in README (good separation)"
fi
    </automated>
  </verify>

  <acceptance_criteria>
    - workspace/README.md contains "sync.md" (reference to sync documentation)
    - workspace/README.md contains "synchronize" or "sync" (concept introduced)
    - workspace/README.md contains "pull request" or "merge to main" (PR-style flow mentioned)
    - workspace/README.md does NOT contain "git commit", "git pull", "git merge --squash" (technical details belong in sync.md)
    - Addition is in "## The Workspace" section, after shared/reference.md paragraph
    - Addition maintains README's philosophical tone (agent perspective, conceptual not procedural)
    - File structure unchanged (no sections removed or reordered)
  </acceptance_criteria>

  <done>
workspace/README.md updated with sync protocol mention. Agents reading the workspace orientation now learn that sync exists, understand the PR-style flow conceptually, and know to read shared/sync.md for detailed git commands. Integration satisfies D-03 (brief mention, link to sync.md) and maintains README's philosophical voice.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. This task only modifies documentation.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Information Disclosure | README describes sync workflow | accept | Workspace is local, accessed only by trusted agents and Ground; no external exposure |
</threat_model>

<verification>
## Overall Phase Checks

After plan completion, verify:

1. **README integration:**
   - Sync protocol mentioned in "The Workspace" section
   - Link to sync.md present
   - Philosophical tone maintained (no technical commands in README)

2. **Documentation coherence:**
   - README provides conceptual overview ("PR-style flow")
   - sync.md provides technical details (git commands)
   - Clear separation of concerns (orientation vs. operations manual)

3. **Requirement satisfaction:**
   - INST-01 (when to commit): README mentions sync exists, sync.md has details
   - INST-02 (when to sync): README explains conceptual flow, sync.md has timing rules
</verification>

<success_criteria>
1. workspace/README.md mentions sync protocol
2. README links to workspace/shared/sync.md for details
3. README explains PR-style flow conceptually (merge to main, partner pulls)
4. README does not contain technical git commands (separation maintained)
5. Addition fits naturally in "The Workspace" section after shared/reference.md
6. Philosophical tone preserved (agent perspective, conceptual framing)
7. INST-01 and INST-02 supported via README → sync.md integration
</success_criteria>

<output>
After completion, create `.planning/phases/03-sync-protocol-documentation/03-02-SUMMARY.md` documenting:
- What was added to README.md (sync mention, link, conceptual explanation)
- How addition maintains README's philosophical tone
- Integration with sync.md (README = orientation, sync.md = operations)
- Which user decision was implemented (D-03)
- How requirements INST-01 and INST-02 are supported by this integration
</output>
